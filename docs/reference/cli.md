# CLI

This page documents the CLI for `disdantic`.

{{ include_file_or_placeholder('.docs/cli.md', '\n*The CLI documentation has not been generated yet. Run `hatch run python:docs` to include it in the build.*\n') }}
